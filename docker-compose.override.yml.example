# Docker Compose Override Example
# Copy this to docker-compose.override.yml for development settings

services:
  backend:
    # Enable hot reload for development
    volumes:
      - ./backend:/app
    environment:
      - DEBUG=true
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload
  
  frontend:
    # Enable hot reload for development
    volumes:
      - ./frontend:/app
    environment:
      - GRADIO_DEBUG=true
